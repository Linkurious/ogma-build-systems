{
  "name": "@linkurious/ogma-build-systems",
  "version": "0.0.1",
  "description": "Build system templates for Ogma.",
  "main": "index.js",
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "build": "npm run prepare && concurrently \"npm run build:vite\" \"npm run build:rollup\" \"npm run build:webpack\" && npm run cleanup",
    "prepare": "node scripts/prepare.js",
    "cleanup": "node scripts/cleanup.js",
    "build:vite": "cd vite && npm i && npm run build",
    "build:rollup": "cd rollup && npm i && npm run build",
    "build:webpack": "cd webpack && npm i && npm run build",
    "postversion": "sync_versions"
  },
  "keywords": [
    "ogma",
    "webpack",
    "vite",
    "template"
  ],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@linkurious/code-tools": "^0.0.14",
    "concurrently": "^7.6.0"
  }
}
